// Declare a function printHello which prints a greeting message.
// Use setTimeout to execute this function after a 3 seconds delay.
// const printHello = () => {
//     console.log('Hi!');
// }
// setTimeout(printHello, 3000);


// -----------------------
// To work on this exercise, it is important to think of functions as regular values (they can be 
// passed around just like any other variable). Revisit the previous section if this wasn't clear.
// Declare a function executeAfterDelay which calls the given function after the given delay in 
// seconds:
// const printHello = () => {
//   console.log('Hello!');
// }
// executeAfterDelay(5, printHello);
// // After about 5 seconds, you should see the message being logged
// Hello!

// const executeAfterDelay = (seconds, aFunction) => {
//     setTimeout(aFunction, seconds * 1000);
// }

// const printHello = () => {
//   console.log('Hello!');
// }
// executeAfterDelay(5, printHello);

// -------------------------------------------

